# Baseline React 18 / Theme 1.0.3 Recovery Report

Report date: 2026-05-31

Workspace:

`C:\Users\Nikola\Documents\Ivan-site-codex-fix`

## Status

**Sprint 0 baseline recovery completed successfully.**

The source tree and regenerated installable WordPress theme ZIP now pass the required React `18.3.1`, React DOM `18.3.1`, and WordPress theme `1.0.3` checks. Sprint 1 was not implemented.

## 1. Changed files

Baseline recovery files:

- `package.json`
- `bun.lock`
- `wordpress-theme/ivan-sedative-theme/style.css`
- `deliverables/baseline-react18-theme103-recovery-report.md`

Generated output refreshed by the theme build and packaging scripts:

- `dist-wp/*`
- `wordpress-theme/ivan-sedative-theme/assets/app/*`
- `wordpress-theme/ivan-sedative-theme.zip`

The generated WordPress JavaScript entry is:

`wordpress-theme/ivan-sedative-theme/assets/app/assets/index-CX7WlKBe.js`

## 2. State after manual Bun install

The repository was inspected after the manual PowerShell command completed successfully:

```powershell
bun install --concurrent-scripts 1
```

Manual command output supplied before this audit:

```text
Checked 510 installs across 646 packages (no changes) [395.00ms]
```

Verified state:

| Check | Result |
| --- | --- |
| Current branch | `main` |
| Bun version | `1.3.14` |
| `package.json` React | `18.3.1` |
| `package.json` React DOM | `18.3.1` |
| `bun.lock` resolved application React | `react@18.3.1` |
| `bun.lock` resolved application React DOM | `react-dom@18.3.1` |
| WordPress theme version | `1.0.3` |
| `package-lock.json` exists | No |
| Root `node_modules` exists | Yes |
| `createRoot` present in `src/wp-entry.tsx` | Yes |
| `hydrateRoot(document)` count in `src/wp-entry.tsx` | `0` |
| `src/lib/interaction-safety.ts` exists | Yes |
| `MobileMenu` interaction safety exists | Yes |

Because `bun.lock` already resolved the required React 18 runtime pair, Codex did not rerun Bun install.

## 3. Exact dependency changes

`package.json` is pinned exactly to:

- `react`: `18.3.1`
- `react-dom`: `18.3.1`

`bun.lock` was regenerated by the successful manual Bun install and now resolves the application runtime pair exactly to:

- `react@18.3.1`
- `react-dom@18.3.1`

No application runtime React 19 dependency remains in `package.json` or `bun.lock`.

## 4. Package lock state

`package-lock.json` was not introduced.

No `npm install`, Yarn, or pnpm command was used.

## 5. Theme version

Verified in `wordpress-theme/ivan-sedative-theme/style.css`:

`Version: 1.0.3`

## 6. Build and ZIP commands

Required project scripts executed successfully:

```powershell
npm run build:theme
npm run zip:theme
```

The desktop shell did not provide npm on PATH, so a temporary npm CLI outside the repository was used only to invoke the existing npm scripts. It did not install project dependencies or create `package-lock.json`.

## 7. Final installer ZIP

Final installable theme ZIP:

`C:\Users\Nikola\Documents\Ivan-site-codex-fix\wordpress-theme\ivan-sedative-theme.zip`

ZIP metadata after packaging:

| Check | Result |
| --- | --- |
| Size | `3214382` bytes |
| Modified time | `2026-05-31T14:50:40.5025913+02:00` |
| ZIP entry count | `35` |

## 8. ZIP verification results

The final installer ZIP itself was opened and inspected.

| Required ZIP check | Result |
| --- | --- |
| `ivan-sedative-theme/style.css` exists | Pass |
| Packaged theme version is `1.0.3` | Pass |
| `ivan-sedative-theme/assets/app/.vite/manifest.json` exists | Pass |
| Manifest entry is `src/wp-entry.tsx` | Pass |
| Built JavaScript exists | Pass |
| `hydrateRoot(document)` count in built JavaScript | `0` |
| `createRoot` exists in built JavaScript | Pass |
| Root-absolute `/assets/` reference count | `0` |
| Built JavaScript contains React `18.3.1` marker | Pass |
| Built JavaScript contains React `19.2.5` marker | No |
| Interaction safety markers remain present | Pass |
| Mobile menu safety marker remains present | Pass |

The packaged interaction safety scan confirmed these markers remain in built JavaScript:

- `ivan_debug`
- `mobile-menu-open`
- `overlay-lock`
- `touchAction`
- `pagehide`
- `popstate`
- `beforeunload`
- `visibilitychange`

## 9. Source verification after build

| Required source check | Result |
| --- | --- |
| `package.json` React is exactly `18.3.1` | Pass |
| `package.json` React DOM is exactly `18.3.1` | Pass |
| `bun.lock` resolved application React is `18.3.1` | Pass |
| `bun.lock` resolved application React DOM is `18.3.1` | Pass |
| `package-lock.json` exists | No |
| `src/wp-entry.tsx` still uses `createRoot` | Pass |
| `hydrateRoot(document)` count in `src/wp-entry.tsx` | `0` |
| `src/lib/interaction-safety.ts` still exists | Pass |
| `unlockBodyInteraction()` remains present | Pass |
| `MobileMenu` interaction safety remains present | Pass |

## 10. Preserved WordPress architecture

The WordPress runtime architecture remains unchanged:

- WordPress entry remains `src/wp-entry.tsx`.
- `createRoot(#root)` architecture remains present.
- `hydrateRoot(document)` remains absent.
- The existing interaction-lock fix remains present.
- No protected source logic file was modified during this recovery pass.

## 11. Warnings and unresolved risks

- Vite completed successfully but emitted a non-blocking CSS optimization warning: the Google Fonts `@import` appears after another generated rule. This recovery pass did not modify typography or stylesheet source.
- `bun.lock` still contains `@types/react@19.2.14` and `@types/react-dom@19.2.3`. These are TypeScript declaration packages, not the application runtime React pair. Runtime React and React DOM are both locked to `18.3.1`.
- WordPress frontend smoke testing was not run in this local recovery pass.

## 12. Sprint 1 confirmation

Sprint 1 was not implemented.

No typography, color, copy, CTA, FAQ, form, layout, content, design, route, calendar, CF7 bridge, WordPress rewrite, or mobile menu behavior change was made.
